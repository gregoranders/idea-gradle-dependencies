//file:noinspection GrUnresolvedAccess

initscript {
  dependencies {
    classpath files('%%GRADLE_DEPENDENCIES_PLUGIN_PATH%%')
  }
}

allprojects {
  apply plugin: '%%GRADLE_DEPENDENCIES_PLUGIN%%'
}
